defaults:
  - agent: mdp_action_recon
  - _self_

task: cheetah_run
obs_type: pixels
pixel_size: 64

num_eval_episodes: 100
seed: 1

val_replay_dir: /home/bmancilla/archive/MaskDP/v_d4rl_data/main/cheetah_run/expert/64px
val_split_ratio: 0.2

eval_snapshots: null

device: cuda
use_tb: false
use_wandb: true
project: maskdp-mm-vd4rl-action-recon
notes: "action reconstruction eval — unimodal MaskedDP"
exp_name: uni_action_recon_second_half_T64