# @package agent
_target_: agent.bc_pixel.BCPixelAgent
name: bc_pixel
obs_shape: ???          
action_shape: ???       
device: ${device}
lr: 1e-4
context_length: 64      # T during finetuning — same as traj_length pretraining
hidden_dim: 256         # dim of BC head
use_tb: ${use_tb}
freeze_encoder: true    # all frozen except BC head
transformer_cfg: null